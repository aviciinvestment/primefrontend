# Copy to .env (not committed) or set in your hosting dashboard.
# Leave unset to use the Express backend's own /api/ai/chat.
# Set to your deployed Cloudflare Worker URL to stream the assistant for free:
# e.g. VITE_AI_CHAT_URL=https://primeopportunity-assistant.YOUR_SUBDOMAIN.workers.dev/api/ai/chat
VITE_AI_CHAT_URL=