# Copy to .env (or set these in your hosting provider's dashboard).
# Vite env vars are public — never put secrets here.

# Origin of the deployed API server, WITHOUT a trailing slash.
# e.g. VITE_API_ORIGIN=https://your-api.onrender.com
VITE_API_ORIGIN=http://localhost:5000